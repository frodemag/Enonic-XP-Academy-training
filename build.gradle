plugins {
    id 'java'
    id 'com.enonic.xp.app' version '1.0.11'
}

xp {
  version = xpVersion
}

app {
    name = project.ext.appName
    displayName = 'Enonic Academy'
    vendorName = 'Enonic AS'
    vendorUrl = 'http://enonic.com'
}

// This is where we "Install" new libraries. Check correct path in our docs, or the libs github readme.
dependencies {
    compile "com.enonic.xp:core-api:${xpVersion}"
    compile "com.enonic.xp:portal-api:${xpVersion}"
    include "com.enonic.xp:lib-content:${xpVersion}"
    include "com.enonic.xp:lib-portal:${xpVersion}"
    include "com.enonic.xp:lib-thymeleaf:${xpVersion}"
    include "com.enonic.xp:lib-mustache:${xpVersion}"
    include "com.enonic.xp:lib-context:${xpVersion}"
    include "com.enonic.xp:lib-auth:${xpVersion}"
    include "com.enonic.lib:util:1.2.0"
    include "com.enonic.lib:menu:1.3.0"
}

repositories {
    mavenLocal()
    jcenter()
    xp.enonicRepo()
}
